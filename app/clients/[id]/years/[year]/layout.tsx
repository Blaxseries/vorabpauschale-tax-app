export const dynamic = "force-dynamic";

type ClientYearLayoutProps = {
  children: React.ReactNode;
};

export default function ClientYearLayout({ children }: ClientYearLayoutProps) {
  return children;
}
